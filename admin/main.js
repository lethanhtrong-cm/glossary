import { initAuth } from './auth.js';

document.addEventListener("DOMContentLoaded", () => {
    // Kích hoạt module Authentication (Tự động kéo theo logic Dashboard khi user đã đăng nhập)
    initAuth();
});import { initAuth } from './auth.js';

document.addEventListener("DOMContentLoaded", () => {
    // Kích hoạt module Authentication (Tự động kéo theo logic Dashboard khi user đã đăng nhập)
    initAuth();
});
